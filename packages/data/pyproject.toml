[project]
name = "regulon-data"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "JuanICasareski", email = "juani.casareski@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
regulon-data = "regulon_data:main"

[build-system]
requires = ["uv_build>=0.11.7,<0.12.0"]
build-backend = "uv_build"
